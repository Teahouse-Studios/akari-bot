# https://toml.io/cn/v1.0.0
# 注意：TOML 不是 Python。請不要在此處使用 Python 語法。
# 例如：TOML 中的布林值必須是小寫。

[module_maimai]
# 模組的組態部分，此處填寫的值可在訊息中以明文顯示。請不要在此部分填寫敏感資訊。
diving_fish_client_id = "<Replace me with str value>"
lxns_client_id = "<Replace me with str value>"

[module_maimai_secret]
# 模組的金鑰組態部分，此處的值若意外出現在傳送的訊息中，機器人會嘗試攔截。但請務必提防洩漏。
diving_fish_client_secret = "<Replace me with str value>" # 水魚帳號 OAuth 應用的 client_secret，公開用戶端可留空。
lxns_developer_token = "<Replace me with str value>" # 落雪咖啡屋查分器的開發者令牌。
lxns_client_secret = "<Replace me with str value>" # 落雪咖啡屋 OAuth 應用的 client_secret。
