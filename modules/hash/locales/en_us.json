{
    "hash.help.desc": "Generate the hash digest of a string.",
    "hash.help.generate": "Generate the hash digest of a string.",
    "hash.help.algorithms": "Returns a list of supported hashing algorithms.",
    "hash.output": "${algorithm} digest of the string is: ${digest}",
    "hash.algorithms": "Supported algorithms: ${algorithms}",
    "hash.unsupported_algorithm": "Algorithm not supported: ${algorithm}"
}
