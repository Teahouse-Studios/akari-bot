"""
临时存储模块 - 用于在会话生命周期内存储临时数据。

该模块提供了一个简单的全局临时存储机制，允许在程序执行期间
临时存储数据，而无需持久化到数据库。
"""


class Temp:
    """
    临时存储类。

    提供一个全局的临时数据存储字典，用于在会话或消息处理期间
    存储临时数据。数据在程序重启后将丢失。

    示例:
    ```
        Temp.data['key'] = 'value'
        value = Temp.data['key']
    ```
    """

    # 存储临时数据的字典，键值对形式存储临时信息
    data = {}


__all__ = ["Temp"]
